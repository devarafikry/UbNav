<FrameLayout
    android:id="@+id/root_map"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:map="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_height="match_parent"
    android:layout_width="match_parent">
    <fragment
              android:id="@+id/map"
              android:name="com.google.android.gms.maps.SupportMapFragment"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              tools:context="devfikr.skripsi.ubnav.MapsActivity"/>
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <android.support.design.widget.FloatingActionButton
            android:src="@drawable/ic_create"
            android:id="@+id/fab_create_path"
            android:onClick="createPath"
            android:layout_margin="10dp"
            android:layout_width="@dimen/floating_button_size"
            android:layout_height="@dimen/floating_button_size"/>

        <android.support.design.widget.FloatingActionButton
            android:src="@drawable/ic_watch"
            android:id="@+id/fab_watch_path"
            android:onClick="watchPath"
            android:visibility="visible"
            android:layout_margin="10dp"
            android:layout_width="@dimen/floating_button_size"
            android:layout_height="@dimen/floating_button_size"/>
        <android.support.design.widget.FloatingActionButton
            android:src="@drawable/ic_watch"
            android:id="@+id/fab_exit_watch_path"
            app:backgroundTint="@android:color/holo_red_light"
            android:onClick="exitWatchPath"
            android:visibility="gone"
            android:layout_margin="10dp"
            android:layout_width="@dimen/floating_button_size"
            android:layout_height="@dimen/floating_button_size"/>

        <android.support.design.widget.FloatingActionButton
            android:src="@drawable/ic_undo"
            android:visibility="gone"
            app:backgroundTint="@color/colorPrimary"
            android:id="@+id/fab_undo"
            android:onClick="undoPath"
            android:layout_margin="10dp"
            android:layout_width="@dimen/floating_button_size"
            android:layout_height="@dimen/floating_button_size"/>
        <android.support.design.widget.FloatingActionButton
            android:src="@drawable/ic_redo"
            android:visibility="gone"
            app:backgroundTint="@color/colorPrimary"
            android:id="@+id/fab_redo"
            android:onClick="redoPath"
            android:layout_margin="10dp"
            android:layout_width="@dimen/floating_button_size"
            android:layout_height="@dimen/floating_button_size"/>
        <android.support.design.widget.FloatingActionButton
            android:src="@drawable/ic_insert_node"
            android:visibility="gone"
            app:backgroundTint="@color/colorPrimary"
            android:id="@+id/fab_edit_status"
            android:onClick="changeEditor"
            android:layout_margin="10dp"
            android:layout_width="@dimen/floating_button_size"
            android:layout_height="@dimen/floating_button_size"/>
        <android.support.design.widget.FloatingActionButton
            android:src="@drawable/ic_clear"
            android:visibility="gone"
            app:backgroundTint="@android:color/holo_red_dark"
            android:id="@+id/fab_clear"
            android:onClick="clearPath"
            android:layout_margin="10dp"
            android:layout_width="@dimen/floating_button_size"
            android:layout_height="@dimen/floating_button_size"/>

    </LinearLayout>

    <Button
        android:layout_margin="10dp"
        android:textColor="@android:color/white"
        android:backgroundTint="@color/colorAccent"
        android:visibility="gone"
        android:id="@+id/btn_finish"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="finishEditor"
        android:text="Finish"
        android:layout_gravity="right"/>

</FrameLayout>